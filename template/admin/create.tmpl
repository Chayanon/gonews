{{define "root-body"}}
<div>
    <h1>Create new Name</h1>
    <form class="ui form" method="POST" enctype="multipart/form-data">
        <dev class="field">
            <label>Title</label>
            <input name="title">
        </dev>
        <dev class="field">
            <label>Image</label>
            <input type="file" name="image">
        </dev>
        <dev class="field">
            <label>Detail</label>
            <textarea name="detail"></textarea>
        </dev>
        <button class="ui blue submit button" type="submit">Create</button>
        <a class="ui red button" href="/admin/list">Cancel</a>
    </form>
</div>
{{end}}

{{define "root-script"}}
<!-- <script>
    console.log("index")
</script> -->
{{end}}

{{define "root-style"}}
{{end}}