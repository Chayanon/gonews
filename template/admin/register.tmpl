{{define "root-body"}}
<div>
    <form class="ui form" method="POST">
        <dev class="field">
            <label>UserName</label>
            <input name="username">
        </dev>
        <dev class="field">
            <label>Password</label>
            <input type="password" name="password">
        </dev>
        <button class="ui submit blue button">Register</button>
    </form>
</div>
{{end}}

{{define "root-script"}}
<script>
    console.log("index")
</script>
{{end}}

{{define "root-style"}}
{{end}}